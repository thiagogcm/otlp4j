/// Built-in count sinks that derive a metric from another signal, built via the `Connectors`
/// factory (`spanCount`, `logRecordCount`) under a configurable [FailurePolicy].
package dev.nthings.otlp4j.connector;
